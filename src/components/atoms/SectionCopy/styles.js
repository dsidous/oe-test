import { createUseStyles } from 'react-jss';

const useStyles = createUseStyles(theme => ({
  title: {
    fontSize: '1rem',
    lineHeight: 1.5,
    margin: 0,
    padding: 0,
  },
}));

export default useStyles;
